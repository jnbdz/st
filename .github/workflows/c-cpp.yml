name: C/C++ CI

on:
  release:
    types: [ "published", "released", "created", "prereleased" ]
    branches: [ "master" ]
  push:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Install packages to build
      run: |
        sudo apt-get install -y libx11-dev pkg-config libharfbuzz-dev libfontconfig-dev libxft-dev build-essential
    - name: make
      run: make
    - name: make archive
      run: make archive
    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: "v0.8.4"
        release_name: Release v0.8.4
        draft: false
        prerelease: false
    - name: Upload Release Asset
      id: upload-release-asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./st-0.8.4.tar.gz
        asset_name: "v0.8.4"
        asset_content_type: application/gzip
